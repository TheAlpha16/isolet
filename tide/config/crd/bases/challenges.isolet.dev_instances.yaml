---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: instances.challenges.isolet.dev
spec:
  group: challenges.isolet.dev
  names:
    kind: Instance
    listKind: InstanceList
    plural: instances
    shortNames:
    - inst
    singular: instance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Challenge Name
      jsonPath: .spec.challenge.name
      name: Challenge
      type: string
    - description: Team ID
      jsonPath: .spec.team.id
      name: Team
      type: string
    - description: Challenge Type
      jsonPath: .spec.challenge.type
      name: Type
      type: string
    - description: Instance Phase
      jsonPath: .status.phase
      name: Phase
      priority: 1
      type: string
    - description: Instance expiry time
      jsonPath: .spec.lifecycle.expiresAt
      name: ExpiresAt
      type: string
    - description: Time since creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Instance is the Schema for the instances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Desired configuration.
            properties:
              challenge:
                description: Challenge backing this instance.
                properties:
                  flag:
                    description: Optional flag injected into the instance as an environment
                      variable.
                    type: string
                  id:
                    description: Unique ID of the challenge.
                    format: int64
                    minimum: 1
                    type: integer
                  image:
                    description: Container image used to run the challenge.
                    type: string
                  slug:
                    description: |-
                      DNS-safe slug derived from the challenge name.
                      Example: "Alice's Adventure" -> "alices-adventure".
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  type:
                    description: How this challenge is provisioned (dynamic or on-demand).
                    enum:
                    - dynamic
                    - on-demand
                    type: string
                required:
                - id
                - image
                - slug
                - type
                type: object
              endpoints:
                description: Network endpoints exposed by this instance.
                items:
                  description: EndpointSpec defines a single network entrypoint for
                    an Instance.
                  properties:
                    name:
                      description: Logical name for the endpoint (e.g., "app", "metrics").
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    protocol:
                      description: Network protocol for this endpoint (e.g., http,
                        ssh).
                      enum:
                      - http
                      - https
                      - nc
                      - ssh
                      type: string
                    targetPort:
                      description: Port on which the application inside the container
                        is listening.
                      format: int32
                      type: integer
                  required:
                  - name
                  - protocol
                  - targetPort
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              lifecycle:
                description: Lifecycle configuration (availability and expiry).
                properties:
                  allowExtension:
                    default: true
                    description: 'Whether expiry can be extended by the user (default:
                      true).'
                    type: boolean
                  availableAt:
                    description: |-
                      Time when the Instance should first become available.
                      If unset, the instance is available immediately after provisioning.
                    format: date-time
                    type: string
                  expiresAt:
                    description: |-
                      Time when the Instance should be terminated.
                      If unset, the instance will run until manually deleted.
                    format: date-time
                    type: string
                type: object
              limits:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Resource limits for CPU/memory.
                type: object
              requests:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Resource requests for CPU/memory.
                type: object
              team:
                description: Team this instance belongs to (not required for dynamic
                  challenges).
                properties:
                  id:
                    description: Unique ID of the team.
                    format: int64
                    minimum: 1
                    type: integer
                required:
                - id
                type: object
            required:
            - challenge
            type: object
          status:
            description: Current status.
            properties:
              conditions:
                description: Standard set of conditions describing resource health
                  and transitions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoints:
                description: Resolved endpoints after provisioning.
                items:
                  description: EndpointStatus defines a resolved network entrypoint
                    for an Instance.
                  properties:
                    hostname:
                      description: |-
                        Fully-qualified domain name. If not provided, the controller will generate one.
                        Example: "<instance-uuid>.<challenge-name>.isolet.dev".
                      type: string
                    name:
                      description: Logical name for the endpoint (e.g., "app", "metrics").
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    port:
                      description: Exposed service port assigned by controller (cluster-facing).
                      format: int32
                      type: integer
                    protocol:
                      description: Network protocol for this endpoint (e.g., http,
                        ssh).
                      enum:
                      - http
                      - https
                      - nc
                      - ssh
                      type: string
                    ready:
                      default: false
                      description: Whether this endpoint is ready to receive traffic.
                      type: boolean
                    targetPort:
                      description: Port on which the application inside the container
                        is listening.
                      format: int32
                      type: integer
                  required:
                  - name
                  - protocol
                  - targetPort
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              phase:
                description: Current lifecycle phase.
                enum:
                - Pending
                - Staged
                - Running
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
