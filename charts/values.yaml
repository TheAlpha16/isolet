# platform unit
platform:
  namespace: platform
  database:
    storageClass: standard
    capacity: 2Gi
  phoros: # fileserver
    storageClass: standard
    capacity: 3Gi

# dynamic challenges unit
dynamic:
  namespace: dynamic

# isolated challenges unit
isolet:
  namespace: isolet

# automation task unit
automated:
  namespace: automated

# admin unit
admin:
  namespace: admin